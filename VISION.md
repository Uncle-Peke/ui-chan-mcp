# ういちゃんMCP — VISION

画面の隅で、ういちゃんが表情を変え、声で喋り、ときどき勝手に動く。
任意のMCP対応エージェントが「体」としてのういを操り、そこにキャラクターが宿る——そういうデスクトップマスコット。

## 何であって、何でないか（設計思想）

このプロジェクトの一番大事な線引きは、**「頭脳」と「体」を分ける**こと。

- **AIエージェント ＝ 頭脳。**
  記憶・状況把握・判断（いつ・なぜ感情を出すか）は、すべてエージェント側の本領。
  エージェントはユーザーが今何をしているかも、これまでの経緯も既に知っている。
- **MCPのうい ＝ 体。**
  表情・声・ポーズという表現のレパートリーと、命令の合間も自律的に生きている体の振る舞い。
  **記憶も知識も持たない。** それは越権であり、頭脳の仕事を体が抱え込むことになる。
- **ペルソナ / context ＝ 接続。**
  頭脳に「その記憶と状況把握を、**ういの体を通して表現しろ**」と促す文章。コードではない。

この分離の帰結:

- **記憶はMCPに持たせない。** エージェントが持つべきもの。
- **状況認識もMCPに作らない。** エージェントは既に状況を知っている。「実際に起きていることに反応しろ」はペルソナで促すことであって、MCPの機能ではない。
- だから、ういが**体として担うべき生命感**は、記憶にも知識にも踏み込まない、**体が勝手に生きている部分**だけになる。そこが体の本分。

## ユビキタス言語

| 用語 | 意味 |
|---|---|
| **Cue** | 表情＋ポーズ＋声色を一括で指定する、完結した見た目の最小単位。1ファイル=1Cue、継承なし。強さ違いは intensity パラメータではなく別のCueファイルとして表現する。AIエージェントは `set_cue` でこの単位を指定する |
| **default Cue** | 全Cueの下地として毎回合成される基準の見た目（Tポーズ相当）。編集対象外の固定ベース |
| **Idling** | 何も演出していない基本状態。Blinkのみ例外的に継続する |
| **IdlingCue** | Idling中に時折差し込まれる、Cue＋セリフの短い自発演出（あくび、鼻歌、呼びかけ等）。Cueのサブタイプ。重み付き抽選＋好感度ゲートで選ばれる |
| **Blink** | まばたき。Idling中・Cue再生中いずれにも独立して重畳される、唯一のCue外ループ |
| **好感度 (affinity)** | ういのユーザーへの好意。セッション内のみ（永続化しない＝毎回つれない所から口説き直す）。方向(up/down)＋強さ(low/middle/high)をエージェントが指定し、実際の増減量は非対称カーブでエンジンが決める（下がりやすく上がりにくい、ツンデレのスロウバーン） |

## 現在の機能

- まばたき（Idling）／リップシンク（音素同期）
- Cue（表情＋ポーズ＋声色）と、Cue切替のクロスディゾルブ
- VoiSona Talk による音声合成
- 好感度による口調・態度の変化、ういビーム
- IdlingCue（自発的な小演出のプール）
- ビジュアルCueエディタ（`npm run editor`）— レイヤーをトグルしてCueを作る

## これから — 「体」の自律的な存在感

「反応する人形」から「一緒にいる存在」への飛躍は、記憶や状況認識（＝頭脳の仕事）ではなく、
**体が命令の合間も勝手に生きていること**で作る。以下はすべて記憶・知識に依存しない、体の管轄。

- **触れられる反応（フィジット）。**
  ホバー・つつく・ドラッグにういが反応する（びっくり／くすぐったがる／むっとする）。
  エージェントが自律稼働している間、ユーザーは手が空いていて暇——そんなとき、つつける相手がいるのは楽しい。ウィンドウが自分へのポインタ操作を拾うだけで成立し、記憶も頭脳も要らない。
- **時間ベースのIdling。**
  実時刻から「おはよう」「おやすみ」「もう寝たら？」のような時間帯の声かけ。
  **AIエージェントに一切依存しない**ので、Idlingの自律演出としてちょうど収まる。
- **気分・生活リズム。**
  時間帯や「今日の気分」がIdlingの振る舞いを色づける。好感度と同じ**セッション状態だけ**で成立し、知識ベースではない。

これらは**好感度と自然に噛み合う**。同じ体の振る舞いが、関係の温度で意味を変える——
同じ「つつく」でも cold なら「は？触んないで」、dere ならくすぐったがってキャッキャ（つつくこと自体が好感度の読み取りになる）。
「おやすみ」も cold なら素っ気なく、dere なら気遣って。気遣いの声かけ（「もう寝たら？」）は高好感度でだけ出す。
しかも**新しい好感度の仕組みは要らない**：IdlingCueには既に `minAffinity`/`maxAffinity` ゲートがあり、触れる反応も「好感度バンドで反応Cueを選ぶ」だけ。既存の好感度が、喋りのトーンに留まらず**すべての体の振る舞いを色づける人格ダイヤル**になる。

### 非目標（越権しない線引き）

- **視線追跡はしない。** エージェントを操作している間ユーザーはマウスを触っていないので、カーソルを目で追っても意味がなく、「何を見ているのか」も伝わらない。
- **記憶・状況認識をMCPに持たせない。** どちらもAIエージェントの本領。体（MCP）はそれを表現する器に徹する。
