node_modules/
dist/
ui-chan-shot.png
.DS_Store
.idea/
.env

# 著作権保護された立ち絵素材はリポジトリに含めない（置き場所を問わず PSD 全般）
*.psd
assets/*.png

# Biome
.biome-cache/

# Claude Code local runtime state (session-specific, not portable)
.claude/
cues.old/

# 更新の記録（npm/コピー導入時にのみ作られる、その環境固有の値）
.ui-chan-install.json
# npm pack の出力
*.tgz
